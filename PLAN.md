1. Add regression coverage for explicit icon dimensions, chart-area SVG scoping, and footer icon-label spacing; commit tests while they fail.
2. Update the icon factory with a 16px default and optional requested size, scope chart canvas sizing to the root SVG, and add shared footer button spacing.
3. Re-check representative swept icon sites, run type-check, lint, and the full test suite, resolve failures, and commit the fix locally.
